# Image Plotter for HTML5 Canvas

Javascript module that does the math to plot image transformations on an HTML5 canvas... so you don't have to.

Currently supports:
* Positioning (cover, contain)
* Zooming
* Moving
